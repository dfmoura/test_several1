/**
 * Enum que representa o status de visita de um vértice durante a busca em largura
 */
public enum StatusVisitaVertice {
    NOVO,        // Vértice ainda não visitado
    VISITADO     // Vértice já visitado
}
