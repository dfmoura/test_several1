# Web (React + Vite)

SPA da instalação **FLEXOERP**. Build de produção entra na imagem nginx (`make web-build` ou `make up`).

Dev isolado: `npm ci && npm run dev` neste diretório, com a API já no ar.
