# Copie para .env e preencha (o Compose lê .env automaticamente).
# CARTO_BASEMAP_API_KEY=

# Idle de sessão (minutos sem request autenticado). Produção recomendado: 30.
# AUTH_SESSION_IDLE_MINUTOS=30
# AUTH_SESSION_DIAS=7
