# Cloudflare Workers — static assets (trigger-site)
# Root directory no dashboard: trigger/12/site
# Deploy command: npx wrangler deploy

name = "trigger-site"
compatibility_date = "2025-06-08"

[assets]
directory = "."
