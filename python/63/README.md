# Criar um programa em Python utilizando Docker

## Objetivo do programa:
Um programa de controle financeiro pessoal em python...
com entrada, atualização, exclusão de dados e tela de interface gráfica, com banco de dados leve

usando dokers


## Comandos Docker:

```bash
docker build -t simple-database .

docker run -it --rm simple-database
```