

### **Sistema de Gestão de Orçamento – Descrição Funcional**

O **Sistema de Gestão de Orçamento** tem como objetivo centralizar e organizar o processo de elaboração, aprovação e controle de orçamentos corporativos de forma simples e eficiente.

---

### **1. Estrutura do Sistema**

#### **1.1. Versão de Orçamento**

* O sistema inicia com a **criação de uma versão de orçamento**, que representa um ciclo orçamentário (ex: Orçamento 2025, Revisão 1, etc.).
* Cada versão pode ser **criada, duplicada, editada ou encerrada**.
* A versão de orçamento funciona como o **nível principal** da estrutura de gestão.

---

#### **1.2. CRs (Centros de Responsabilidade)**

* Cada versão de orçamento contém **vários CRs** (Centros de Responsabilidade).
* Cada CR possui:

  * Um **responsável designado** (usuário do sistema).
  * Um **status** (em edição, submetido, aprovado, rejeitado).
  * Um **histórico de alterações** (quem modificou, quando e o que foi alterado).
* O responsável por cada CR é o ponto de controle para inserir, revisar e submeter os dados do seu centro.

---

#### **1.3. Plano de Contas**

* Dentro de cada CR, há uma **estrutura de contas** conforme o **plano de contas da empresa**.
* Cada conta possui:

  * **Código e descrição da conta**.
  * **Valor proposto**.
  * **Valor aprovado** (após validação).
  * **Observações ou justificativas** opcionais.

---

### **2. Fluxo de Interações**

#### **2.1. Criação**

1. O administrador cria uma **nova versão de orçamento**.
2. Define os **CRs** e **responsáveis** associados.
3. O sistema gera automaticamente as **contas padrão** do plano de contas para cada CR.

#### **2.2. Edição e Submissão**

1. Cada responsável acessa seu CR.
2. Preenche ou ajusta os valores por conta.
3. Submete o CR para aprovação.

#### **2.3. Aprovação**

1. O aprovador (gestor ou controladoria) revisa as informações submetidas.
2. Pode **aprovar**, **rejeitar** ou **solicitar ajustes**.
3. O sistema registra todas as ações em **log de auditoria**.

#### **2.4. Consolidação**

* Após a aprovação de todos os CRs, o sistema permite a **consolidação total** da versão do orçamento, exibindo relatórios e totais por:

  * CR
  * Conta
  * Centro de custo
  * Categoria ou tipo de despesa

---

### **3. Funcionalidades Adicionais**

* **Histórico de versões**: consulta a versões anteriores e comparação entre elas.
* **Controle de permissões**: perfis de acesso diferenciados (administrador, responsável, aprovador, visualizador).
* **Exportação de dados**: planilhas e relatórios em PDF ou Excel.
* **Painel de status**: visão geral de progresso (quantos CRs pendentes, aprovados, rejeitados).
* **Notificações automáticas**: alertas por e-mail ou sistema (ex: “CR aprovado”, “versão encerrada”).

---

### **4. Benefícios**

* Simplifica o processo de elaboração orçamentária.
* Centraliza informações e evita planilhas dispersas.
* Permite rastreabilidade e controle de alterações.
* Facilita a análise consolidada e a tomada de decisão.

---



---

## 🧩 Descrição do problema

* O sistema possui **versões de orçamento**.
* Cada **versão** contém vários **CRs** (Centros de Responsabilidade).
* Cada **CR** possui um **responsável** (usuário).
* Cada **CR** tem várias **contas** do **plano de contas**, com **valores orçados** (a serem aprovados).

---

## 🗂️ Modelo de Tabelas (MySQL)

### 1️⃣ `versao_orcamento`

Guarda as versões do orçamento criadas.

```sql
CREATE TABLE versao_orcamento (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(100) NOT NULL,
    descricao TEXT,
    data_criacao DATETIME DEFAULT CURRENT_TIMESTAMP,
    status ENUM('rascunho', 'em_aprovacao', 'aprovado', 'rejeitado') DEFAULT 'rascunho'
);
```

---

### 2️⃣ `responsavel`

Tabela de responsáveis (usuários do sistema).

```sql
CREATE TABLE responsavel (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(100) NOT NULL,
    email VARCHAR(150) UNIQUE,
    cargo VARCHAR(100),
    ativo BOOLEAN DEFAULT TRUE
);
```

---

### 3️⃣ `cr` (Centro de Responsabilidade)

Cada CR pertence a uma **versão de orçamento** e tem um **responsável**.

```sql
CREATE TABLE cr (
    id INT AUTO_INCREMENT PRIMARY KEY,
    codigo VARCHAR(20) NOT NULL,
    nome VARCHAR(100) NOT NULL,
    id_versao INT NOT NULL,
    id_responsavel INT,
    FOREIGN KEY (id_versao) REFERENCES versao_orcamento(id),
    FOREIGN KEY (id_responsavel) REFERENCES responsavel(id)
);
```

---

### 4️⃣ `plano_contas`

Plano de contas padrão (estrutura contábil).

```sql
CREATE TABLE plano_contas (
    id INT AUTO_INCREMENT PRIMARY KEY,
    codigo VARCHAR(20) NOT NULL,
    descricao VARCHAR(150) NOT NULL,
    tipo ENUM('receita', 'despesa', 'outro') DEFAULT 'despesa'
);
```

---

### 5️⃣ `cr_conta`

Tabela que relaciona **CRs** com **contas do plano de contas** e armazena **valores orçados**.

```sql
CREATE TABLE cr_conta (
    id INT AUTO_INCREMENT PRIMARY KEY,
    id_cr INT NOT NULL,
    id_plano_conta INT NOT NULL,
    valor_orcado DECIMAL(15,2) DEFAULT 0,
    valor_aprovado DECIMAL(15,2) DEFAULT 0,
    observacao VARCHAR(255),
    FOREIGN KEY (id_cr) REFERENCES cr(id),
    FOREIGN KEY (id_plano_conta) REFERENCES plano_contas(id)
);
```

---

### 6️⃣ (Opcional) `historico_aprovacao`

Caso queira registrar quem aprovou e quando.

```sql
CREATE TABLE historico_aprovacao (
    id INT AUTO_INCREMENT PRIMARY KEY,
    id_versao INT NOT NULL,
    id_responsavel INT NOT NULL,
    data_aprovacao DATETIME DEFAULT CURRENT_TIMESTAMP,
    status ENUM('aprovado', 'rejeitado') NOT NULL,
    comentario TEXT,
    FOREIGN KEY (id_versao) REFERENCES versao_orcamento(id),
    FOREIGN KEY (id_responsavel) REFERENCES responsavel(id)
);
```

---

## 📊 Diagrama lógico (resumo das relações)

```
versao_orcamento ───< cr ───< cr_conta >─── plano_contas
                         │
                         └─── responsavel
```

---

## ✅ Benefícios do modelo

* Permite múltiplas versões de orçamento.
* Cada versão é independente (com seus CRs e valores).
* Escalável para incluir histórico de aprovações, revisões e workflows.
* Facilita consultas agregadas (por CR, por conta, por versão etc.).

---

Quer que eu monte também o **script SQL completo** (com `INSERT` de exemplo e algumas consultas padrão, como totais por CR ou por versão)?



